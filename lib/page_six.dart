import 'package:flutter/material.dart';

class PageSix extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Page six'),
      ),
      body: Center(
        child: Text('This is Page six'),
      ),
    );
  }
}
